#include "ginfer/op/layer.h"

namespace ginfer::op {

// MatmulLayer::MatmulLayer(DeviceType dev_type, std::string layer_name, int32_t m, )
//     : Layer(dev_type, LayerType::kLayerMatmul, std::move(layer_name)) {}

// Status MatmulLayer::forward(std::vector<const Tensor*> inputs, Tensor* output) {
//   // Implement the forward pass for the Matmul layer
//   return ginfer::error::Success();
// }

};  // namespace ginfer::op